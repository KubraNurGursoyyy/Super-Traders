'use strict';
const express = require('express');
/*
const PortfolioRoutes =  ('./modules/portfolio');
const SharesRoutes = ('./modules/shares');
const UserRoutes = ('./modules/user');
const WalletRoutes = ('./modules/wallet');*/
const app = express();

app.use(express.json());
app.use(express.urlencoded({ extended: true }));
/*
app.use(app.router);
[PortfolioRoutes, SharesRoutes, UserRoutes,WalletRoutes].initialize(app);*/

const PORT = process.env.PORT || 8080;
app.listen(PORT, () => {
  console.log(`Server listening on port ${PORT}...`);
});




